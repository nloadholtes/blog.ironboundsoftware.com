name: Possibility and Probability
description: Trying out markdown for everything

permalink: /:year/:month/:title
baseurl: 

markdown: kramdown
kramdown:
  input: GFM # Enable GitHub Flavored Markdown (fenced code blocks)
  hard_wrap: true
  
remote_theme: nloadholtes/jekyll-theme-hacker

plugins:
  - jekyll-sitemap
